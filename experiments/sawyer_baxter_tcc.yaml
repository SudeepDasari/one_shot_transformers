batch_size: 12
lr: 0.001
epochs: 200
loader_workers: 4
save_freq: 5000

dataset:
  type: "agent teacher"
  agent_dir: ${ROBOT_DIR}/sawyer_can
  teacher_dir: ${ROBOT_DIR}/baxter_*
  N_pair: 0


model:
  type: vgg
