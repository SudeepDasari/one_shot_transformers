batch_size: 3
lr: 0.001
log_sigma_weight: 0.0
normalize_sigma_square: False

dataset:
  type: "something something"
  root_dir: ${SOMETHING_DIR}
  T: 15
  key_filter: "putting [something] on a surface"

model:
  type: base
