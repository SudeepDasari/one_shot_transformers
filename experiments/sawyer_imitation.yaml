batch_size: 8
lr: 0.0001
epochs: 200
loader_workers: 40

dataset:
  type: "agent"
  root_dir: ${ROBOT_DIR}/sawyer
  height: 112
  width: 112
  T_context: 0
  T_pair: 20

model:
  type: "base rnn"
  in_dim: 512
  out_dim: 64
  visual_input: True

mdn:
  in_dim: 64
  out_dim: 8
