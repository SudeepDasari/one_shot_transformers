batch_size: 16
lr: 0.001
epochs: 400
loader_workers: 8

dataset:
  type: "agent"
  root_dir: ${ROBOT_DIR}/sawyer
  height: 224
  width: 224
  T_context: 0
  T_pair: 20

embed:
  type: ''
  
rnn:
  type: "base rnn"
  in_dim: 512
  out_dim: 64
  visual_input: True

mdn:
  in_dim: 64
  out_dim: 8
